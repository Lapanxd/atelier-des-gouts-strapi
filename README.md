# Atelier des gouts strapi

## Installation
> Create a  `.env` at the root of the project, and copy the content of the `.env.example` inside.

```bash
# install dependancies
npm install

# run the project
npm run start

# run the project (dev mode)
npm run dev
``` 